import 'package:flutter_config/flutter_config.dart';

class Connection {
  //static final String baseUrl = "http://localhost" + ":8080";
  //static final baseUrl = "http://" + FlutterConfig.get('IP') + ":8080";
  static const String baseUrl = "http://10.0.2.2" + ":8080";
}
